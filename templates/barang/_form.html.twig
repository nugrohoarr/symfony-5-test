{% block body %}
<div class="section-header">
    <h1>{{ form.vars.value.idBarang ? 'Edit barang' : 'Tambah barang' }}</h1>
</div>
<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-header">
                <h4>{{ form.vars.value.idBarang ? 'Edit barang' : 'Tambah barang' }}</h4>
            </div>
            <div class="card-body">
                {{ form_start(form) }}
                {% for message in app.flashes('success') %}
                    <div class="alert alert-success">{{ message }}</div>
                {% endfor %}
                {% for message in app.flashes('error') %}
                    <div class="alert alert-danger">{{ message }}</div>
                {% endfor %}
                <div class="row">
                    <div class="col-lg-12">
                        <div class="form-group">
                            <label>Nama Barang</label>
                            {{ form_widget(form.namaBarang, {'attr': {'class': 'form-control'}}) }}
                        </div>
                        <div class="form-group text-right">
                            <button class="btn btn-info mr-1" type="submit">Simpan</button>
                            <a href="{{ path('app_barang_index') }}"><button type="button" class="btn btn-danger" name="batal">Kembali</button></a>
                        </div>
                    </div>
                </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
