{% extends 'base.html.twig' %}

{% block title %}Barang{% endblock %}

{% block body %}
    <h1>Barang</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ barang.id }}</td>
            </tr>
            <tr>
                <th>NamaBarang</th>
                <td>{{ barang.namaBarang }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_barang_index') }}">back to list</a>

    <a href="{{ path('app_barang_edit', {'id': barang.id}) }}">edit</a>

    {{ include('barang/_delete_form.html.twig') }}
{% endblock %}
